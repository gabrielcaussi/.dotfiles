[module/cpu]
    type = internal/cpu

    interval = 2

    format-prefix = 
    format-prefix-foreground = ${colors.primary}

    label = %percentage:2%%
    label-foreground = ${colors.foreground}
