[module/ethernet]
type = internal/network

interface = enp5s0

interval = 3

format-connected-prefix = " "
format-connected-prefix-foreground = ${colors.foreground}

label-connected = " "

format-disconnected = ""

